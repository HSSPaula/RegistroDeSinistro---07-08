@model List<Sinistro>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <table class="table">
        <tr>
            <th>Data</th>
            <th>Tipo</th>
            <th>Local</th>
            <th colspan="2">Ações</th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Data.ToString("dd/MM/yyyy")</td>
                <td>@item.Tipo</td>
                <td>@(item.Rua + ", " + item.Numero + " - " + item.Bairro + " - " + item.Cidade + ", " + item.Estado + " - " + item.Pais)</td>
                <td><a asp-controller="Home" asp-action="EditSinistro" asp-route-id="@item.Id" class="btn btn-primary">Editar</a></td>
                <td><a asp-controller="Home" asp-action="DeleteSinistro" asp-route-id="@item.Id" class="btn btn-danger">Excluir</a></td>
            </tr>
        }
       
       
    </table>
</div>
